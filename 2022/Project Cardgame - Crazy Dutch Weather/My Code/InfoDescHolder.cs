using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InfoDescHolder : MonoBehaviour
{
    // Was or still is used to find objects in unity.
    // I use this instead of the string search methods. Hence why I haven't used tags or layers this project

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
